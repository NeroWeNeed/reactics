using Unity.Entities;

namespace Reactics.Core.UI {
    /*     public struct SimpleUILayoutProperties : IComponentData {
            public int priority;
            public bool visible;
            public bool partOfLayout;

            public enum Direction {
                Row = 0x00, Column = 0x01, RowReversed = 0x10, ColumnReversed = 0x11
            }

        }
        public static class SimpleUILayoutExtensions {
            public static bool IsRow(this SimpleUILayoutProperties.Direction self) => ((byte)self & 0x01) == 0;
            public static bool IsColumn(this SimpleUILayoutProperties.Direction self) => ((byte)self & 0x01) != 0;
            public static bool IsReversed(this SimpleUILayoutProperties.Direction self) => ((byte)self & 0x10) != 0;
        } */
}